Importing csv to FreezerPro needs to be limited, depending on the number of fields in csv, to around 1000 lines per file and using the API to import is a major PITA.

The script allows you to create files based on chunk size and spit out those files with naming based on what lines are included.
